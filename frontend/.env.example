# Laravel Reverb (WebSocket) — must match backend REVERB_* values.
# The browser connects directly to the Reverb server (exposed on :8080).
VITE_REVERB_APP_KEY=kitchens-key
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
